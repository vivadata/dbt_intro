sources:
  - name: stripe
    database: dbt-tutorial
    schema: stripe
    tables:
      - name: payment
        config:
          freshness:
            warn_after:
              count: 12
              period: hour
            error_after:
              count: 2
              period: day
          loaded_at_field: _batched_at